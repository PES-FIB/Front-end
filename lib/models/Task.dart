class Task{

  String name, description, initial_date, final_date, repeats;
  int id;
  int code;
  Task(this.id, this.code, this.name, this.description, this.initial_date, this.final_date, this.repeats);
}